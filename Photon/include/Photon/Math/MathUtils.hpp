#pragma once

namespace Photon
{
    /// @brief Converts the provided angle in degrees to radians.
    /// @param angle The input angle represented as degrees.
    /// @return Outputs the angle converted to radians.
    float degToRad(float angle);
}